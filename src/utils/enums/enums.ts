export enum applicationView {
  Home = 'Home',
  AboutUs = 'About Us',
  Skills = 'Skills',
  Contact = 'Contact',
};

export enum Alignment {
  Left = 'left',
  Center = 'center',
  Right = 'right',
  Top = 'top',
  Bottom = 'bottom'
}

export enum TextAlignment {
  Left = 'left',
  Center = 'center',
  Right = 'right'
}

export enum HeadlineType {
  Underline = 'underline',
  Overlay = 'overlay'
}
